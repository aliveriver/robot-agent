
## 框架结构总览

```
robot-agent/
├── .env.example          ← 所有配置项模板（复制为 .env 填写）
├── requirements.txt      ← 依赖包
├── configs/
│   ├── app.yaml          ← 主配置（唤醒词、记忆阈值、Tool 开关等）
│   └── prompts/          ← 全部 prompt 外置文件
│       ├── base_cn.txt / base_en.txt
│       ├── multimodal_cn.txt
│       └── memory_extract.txt
└── src/robot_agent/
    ├── app.py            ← 启动入口（跑通即可验证框架）
    ├── settings.py       ← 统一配置加载
    ├── graph/            ← LangGraph 核心
    │   ├── state.py      ← AgentState 定义
    │   ├── agent_graph.py← 节点串联 + 条件路由
    │   └── nodes/        ← 8 个节点，每个独立文件
    ├── capabilities/     ← 可替换能力层
    │   ├── asr/          ← SherpaRecognizer + 文本清洗工具
    │   ├── tts/          ← KokoroTTS
    │   ├── llm/          ← ChatModel 封装
    │   ├── memory/       ← ShortTermMemory
    │   └── vision/       ← RosCameraProvider
    ├── tools/            ← Tool 注册表 + 内置 Tool
    └── interfaces/       ← 麦克风等设备接口
```

## 接下来怎么做

每个文件里带 `# TODO:` 注释的地方就是后续 agent 需要填充的实现点，优先级顺序：

| 优先级 | 文件 | 任务 | 完成 |
|---|---|---|---|
| 1 | `capabilities/asr/sherpa_adapter.py` | 迁移 Sherpa 识别逻辑 | finished |
| 1 | `capabilities/tts/kokoro_adapter.py` | 迁移 Kokoro WebSocket TTS | finished |
| 2 | `graph/nodes/response_gen.py` | 接入 LLM 调用 | finished |
| 2 | `interfaces/audio/microphone.py` | 迁移麦克风采集 + VAD | finished |
| 3 | `graph/nodes/memory_recall.py` | 接入 SQLite 记忆存储 | finished |
| 3 | `graph/nodes/kb_retrieve.py` | 接入 Chroma 检索 | |

你可以先运行 `python -m src.robot_agent.app` 验证框架流程跑通（会走完整 Graph，输出 stub 回复），然后逐步替换 TODO 部分。